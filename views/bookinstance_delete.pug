extends layout

block content

    h1 #{title}: #{bookinstance.book.title}

    p Do you really want to delete this Book Instance?

    form(method='POST')
        div.form-group
            input#authorid.form-control(type='hidden' name='authorid', value=bookinstance._id )

        button.btn.btn-primary(type='submit') Delete
